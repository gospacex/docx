package couchbase

// Version is the released semantic version of the couchbase sub-module.
// It tracks the docx public-layer version (see README badge) so consumers
// can pin a single version across the whole hubx/cache monorepo.
const Version = "0.2.0-dev"
